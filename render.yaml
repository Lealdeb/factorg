services:
  - type: web
    name: restaurant-api
    env: python
    rootDir: backend_restaurant_xml_corr
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host=0.0.0.0 --port=10000
    envVars:
      - key: DATABASE_URL
        value: postgresql://postgres:0739Airg----@db.saqsmmkzxljrnsmcplxt.supabase.co:5432/postgres
